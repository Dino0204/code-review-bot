# GSML API 키 — https://gsmsv.site 에서 발급
GSML_API_KEY=

# 로컬 실행(npm run review)용 GitHub 토큰. Actions 안에서는 GITHUB_TOKEN이 자동 주입된다.
GITHUB_TOKEN=

# 선택: 기본값을 덮어쓸 때만 지정
# REVIEWBOT_MODEL=darwin-35b-q4_k_m.gguf
# REVIEWBOT_BASE_URL=http://ssh.gsmsv.site:26145/v1
# REVIEWBOT_LANGUAGE=ko

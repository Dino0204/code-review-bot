# z.ai(GLM) API 키 — https://z.ai/manage-apikey/apikey-list 에서 발급
ZAI_API_KEY=

# 로컬 실행(npm run review)용 GitHub 토큰. Actions 안에서는 GITHUB_TOKEN이 자동 주입된다.
GITHUB_TOKEN=

# 선택: 기본값을 덮어쓸 때만 지정
# REVIEWBOT_MODEL=glm-4.7-flash
# REVIEWBOT_BASE_URL=https://api.z.ai/api/paas/v4
# REVIEWBOT_LANGUAGE=ko

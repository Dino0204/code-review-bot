{
  "name": "gsml-code-review-bot",
  "version": "0.1.0",
  "description": "GSML 게이트웨이 기반 GitHub 코드 리뷰 봇 — gemini-code-assist 대체용",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test tests/*.test.ts",
    "review": "node --env-file-if-exists=.env --import tsx src/cli.ts"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/it-play/Code-Review-Bot.git"
  },
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^3.0.1",
    "@octokit/rest": "^22.0.1",
    "minimatch": "^10.2.6",
    "yaml": "^2.9.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "esbuild": "^0.28.1",
    "tsx": "^4.23.8",
    "typescript": "^7.0.2"
  }
}
